// `/pengelola` → halaman utama Validasi Laporan (ROUTES §5).
import { Navigate } from "react-router";

export default function Route() {
  return <Navigate to="/pengelola/validasi-laporan" replace />;
}
