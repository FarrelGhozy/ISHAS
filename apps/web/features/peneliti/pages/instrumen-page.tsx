import { useState } from "react";
import { storeActions, useMockState } from "~/mocks/store/mock-store";
import { StatusChip } from "~/shared/components/status-chip";

export function Page() {
  const state = useMockState();
  const [versionId, setVersionId] = useState(state.activeInstrumentVersionId ?? state.instrumentVersions[0]?.id ?? "");
  const version = state.instrumentVersions.find((x) => x.id === versionId);
  const [dimName, setDimName] = useState("");
  const [note, setNote] = useState("");
  const [indForm, setIndForm] = useState<Record<string, { code: string; title: string; prompt: string; answerType: "likert-1-5" | "boolean-ya-tidak" | "likert-1-2-tidak"; required: boolean; evidenceRequired: boolean; locationRequired: boolean }>>({});
  const isDraft = version?.status === "Draft";

  const addDim = () => {
    if (!version) return;
    const r = storeActions.addInstrumentDimension(version.id, dimName);
    setNote(r.ok ? `Dimensi ditambahkan ke ${version.id}.` : r.error);
    if (r.ok) setDimName("");
  };

  const addInd = (dimId: string) => {
    if (!version) return;
    const f = indForm[dimId] ?? { code: "", title: "", prompt: "", answerType: "likert-1-5" as const, required: true, evidenceRequired: false, locationRequired: false };
    const r = storeActions.addInstrumentIndicator(version.id, dimId, f);
    setNote(r.ok ? `Indikator ${f.code || "baru"} ditambahkan.` : r.error);
    if (r.ok) setIndForm((old) => ({ ...old, [dimId]: { code: "", title: "", prompt: "", answerType: "likert-1-5", required: true, evidenceRequired: false, locationRequired: false } }));
  };

  return <section className="flex flex-col gap-4"><header><p className="kicker">Metodologi</p><h1 className="text-2xl font-extrabold text-heading">Instrumen penelitian</h1><p className="text-sm text-secondary-text">Tinjau struktur dimensi, indikator, dan aturan pengumpulan bukti. Versi Published terkunci; perubahan hanya pada Draft (FLOWS §7).</p></header><label className="max-w-sm text-xs font-bold">Versi instrumen<select className="mt-1 min-h-11 w-full rounded border border-line-soft bg-white px-3" value={versionId} onChange={(e) => setVersionId(e.target.value)}>{state.instrumentVersions.map((x) => <option value={x.id} key={x.id}>{x.label} · {x.status}</option>)}</select></label>{note ? <p role="status" className="text-sm">{note}</p> : null}{version && <><div className="surface flex flex-wrap items-center gap-3 p-4"><div className="mr-auto"><strong className="text-heading">{version.label}</strong><p className="text-xs text-secondary-text">{version.dimensions.length} dimensi · {version.dimensions.flatMap((x) => x.indicators).length} indikator</p></div><StatusChip value={version.status} /></div>{isDraft ? <div className="surface flex flex-wrap items-end gap-3 p-4"><label className="min-w-60 flex-1 text-xs font-bold">Dimensi baru<input className="mt-1 min-h-11 w-full rounded border border-line-soft px-3 font-normal" value={dimName} onChange={(e) => setDimName(e.target.value)} placeholder="Contoh: Keselamatan listrik" /></label><button type="button" className="primary-button" onClick={addDim}>Tambah dimensi</button></div> : <p className="rounded-lg border border-line bg-strip p-3 text-sm text-secondary-text">Versi {version.status} terkunci. Buat draft baru di Versioning untuk mengubah struktur.</p>}{version.dimensions.map((d) => <article className="surface overflow-hidden" key={d.id}><div className="border-b border-line bg-strip px-4 py-3"><h2 className="font-bold text-heading">{d.name}</h2><p className="text-xs text-faint">{d.id} · {d.indicators.length} indikator</p></div><div className="divide-y divide-line">{d.indicators.map((i) => <div className="p-4" key={i.id}><div className="flex flex-wrap gap-2"><strong className="mr-auto text-sm text-heading">{i.code} · {i.title}</strong>{i.required && <span className="status status-neutral">Wajib</span>}{i.evidenceRequired && <span className="status status-amber">Bukti wajib</span>}{i.locationRequired && <span className="status status-blue">Lokasi wajib</span>}</div><p className="mt-1 text-sm text-secondary-text">{i.prompt}</p><p className="mt-2 text-xs text-faint">Jawaban: {i.answerType} · Pemicu temuan: {i.findingTrigger}</p></div>)}</div>{isDraft ? <div className="grid gap-2 border-t border-line bg-strip p-4 md:grid-cols-3"><label className="text-xs font-bold">Kode<input className="mt-1 min-h-10 w-full rounded border border-line-soft px-2 font-normal" value={indForm[d.id]?.code ?? ""} onChange={(e) => setIndForm((o) => ({ ...o, [d.id]: { code: e.target.value, title: o[d.id]?.title ?? "", prompt: o[d.id]?.prompt ?? "", answerType: o[d.id]?.answerType ?? "likert-1-5", required: o[d.id]?.required ?? true, evidenceRequired: o[d.id]?.evidenceRequired ?? false, locationRequired: o[d.id]?.locationRequired ?? false } }))} placeholder="IND-XXX-000" /></label><label className="text-xs font-bold">Judul<input className="mt-1 min-h-10 w-full rounded border border-line-soft px-2 font-normal" value={indForm[d.id]?.title ?? ""} onChange={(e) => setIndForm((o) => ({ ...o, [d.id]: { code: o[d.id]?.code ?? "", title: e.target.value, prompt: o[d.id]?.prompt ?? "", answerType: o[d.id]?.answerType ?? "likert-1-5", required: o[d.id]?.required ?? true, evidenceRequired: o[d.id]?.evidenceRequired ?? false, locationRequired: o[d.id]?.locationRequired ?? false } }))} /></label><label className="text-xs font-bold">Tipe jawaban<select className="mt-1 min-h-10 w-full rounded border border-line-soft px-2 font-normal" value={indForm[d.id]?.answerType ?? "likert-1-5"} onChange={(e) => setIndForm((o) => ({ ...o, [d.id]: { code: o[d.id]?.code ?? "", title: o[d.id]?.title ?? "", prompt: o[d.id]?.prompt ?? "", answerType: e.target.value as "likert-1-5", required: o[d.id]?.required ?? true, evidenceRequired: o[d.id]?.evidenceRequired ?? false, locationRequired: o[d.id]?.locationRequired ?? false } }))}><option value="likert-1-5">likert-1-5</option><option value="boolean-ya-tidak">boolean-ya-tidak</option><option value="likert-1-2-tidak">likert-1-2-tidak</option></select></label><label className="text-xs font-bold md:col-span-3">Prompt<textarea className="mt-1 min-h-16 w-full rounded border border-line-soft p-2 font-normal" value={indForm[d.id]?.prompt ?? ""} onChange={(e) => setIndForm((o) => ({ ...o, [d.id]: { code: o[d.id]?.code ?? "", title: o[d.id]?.title ?? "", prompt: e.target.value, answerType: o[d.id]?.answerType ?? "likert-1-5", required: o[d.id]?.required ?? true, evidenceRequired: o[d.id]?.evidenceRequired ?? false, locationRequired: o[d.id]?.locationRequired ?? false } }))} placeholder="Tulis pertanyaan observasi min 10 karakter" /></label><div className="flex flex-wrap gap-3 text-xs md:col-span-2"><label className="flex items-center gap-1"><input type="checkbox" checked={indForm[d.id]?.required ?? true} onChange={(e) => setIndForm((o) => ({ ...o, [d.id]: { code: o[d.id]?.code ?? "", title: o[d.id]?.title ?? "", prompt: o[d.id]?.prompt ?? "", answerType: o[d.id]?.answerType ?? "likert-1-5", required: e.target.checked, evidenceRequired: o[d.id]?.evidenceRequired ?? false, locationRequired: o[d.id]?.locationRequired ?? false } }))} />Wajib</label><label className="flex items-center gap-1"><input type="checkbox" checked={indForm[d.id]?.evidenceRequired ?? false} onChange={(e) => setIndForm((o) => ({ ...o, [d.id]: { code: o[d.id]?.code ?? "", title: o[d.id]?.title ?? "", prompt: o[d.id]?.prompt ?? "", answerType: o[d.id]?.answerType ?? "likert-1-5", required: o[d.id]?.required ?? true, evidenceRequired: e.target.checked, locationRequired: o[d.id]?.locationRequired ?? false } }))} />Bukti wajib</label><label className="flex items-center gap-1"><input type="checkbox" checked={indForm[d.id]?.locationRequired ?? false} onChange={(e) => setIndForm((o) => ({ ...o, [d.id]: { code: o[d.id]?.code ?? "", title: o[d.id]?.title ?? "", prompt: o[d.id]?.prompt ?? "", answerType: o[d.id]?.answerType ?? "likert-1-5", required: o[d.id]?.required ?? true, evidenceRequired: o[d.id]?.evidenceRequired ?? false, locationRequired: e.target.checked } }))} />Lokasi wajib</label></div><button type="button" className="secondary-button self-end" onClick={() => addInd(d.id)}>Tambah indikator</button></div> : null}</article>)}</>}</section>;
}
