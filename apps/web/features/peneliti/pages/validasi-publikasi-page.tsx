import { useMockState } from "~/mocks/store/mock-store";
import { StatusChip } from "~/shared/components/status-chip";

export function Page() {
  const state=useMockState();const assessments=state.reports.filter(x=>x.channel==="penilaian-mandiri");
  return <section className="flex flex-col gap-4"><header><p className="kicker">Kualitas data</p><h1 className="text-2xl font-extrabold text-heading">Validasi & publikasi</h1><p className="text-sm text-secondary-text">Pantau kelengkapan snapshot dan kelayakannya sebagai sumber hasil publik.</p></header><div className="scope-banner">Keputusan menerima atau menolak laporan tetap berada pada Pengelola Pesantren sesuai lingkup lembaga. Peneliti mengaudit kesiapan publikasi.</div><div className="surface divide-y divide-line">{assessments.map(x=>{const snapshot=state.selfAssessmentSnapshots.find(s=>s.reportId===x.id);const version=state.instrumentVersions.find(v=>v.id===snapshot?.instrumentVersionId);const expected=version?.dimensions.flatMap(d=>d.indicators).length??0;const answered=snapshot?Object.values(snapshot.answers).filter(a=>a.value).length:0;const complete=Boolean(snapshot&&expected>0&&answered===expected);return <article className="flex flex-wrap items-center gap-3 p-4" key={x.id}><div className="mr-auto"><strong className="text-heading">{x.id} · {state.institutions.find(i=>i.code===x.institutionCode)?.name}</strong><p className="text-xs text-secondary-text">{snapshot?.instrumentVersionId??"Tanpa snapshot"} · {answered}/{expected} jawaban terisi</p><p className="text-xs text-faint">{x.validationStatus==="Diterima"&&complete?"Layak menjadi sumber publik":"Belum menjadi sumber publik"}</p></div><StatusChip value={x.validationStatus}/><span className={`status ${complete?"status-green":"status-red"}`}>{complete?"Lengkap":"Tidak lengkap"}</span></article>})}</div></section>;
}
