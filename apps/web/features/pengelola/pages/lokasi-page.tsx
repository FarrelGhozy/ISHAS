import { useState } from "react";
import { storeActions, useMockState } from "~/mocks/store/mock-store";
import { useCurrentUser } from "~/shared/auth/use-current-user";
import { EmptyState } from "~/shared/components/empty-state";

const field = "mt-1 min-h-11 w-full rounded border border-line-soft px-3";

export function Page() {
  const state = useMockState(); const user = useCurrentUser();
  const [code, setCode] = useState(""); const [name, setName] = useState(""); const [buildingId, setBuildingId] = useState(""); const [floor, setFloor] = useState("Lantai 1"); const [area, setArea] = useState(""); const [zone, setZone] = useState(""); const [newFloor, setNewFloor] = useState(""); const [message, setMessage] = useState("");
  if (!user || user.roleId !== "pengelola" || user.institutionCodes.length !== 1) return <EmptyState title="Halaman ini hanya untuk Pengelola Pesantren" />;
  const buildings = state.buildings.filter((x) => x.institutionCode === user.institutionCodes[0]); const areas = state.areas.filter((x) => x.institutionCode === user.institutionCodes[0]);
  const run = (action: () => { ok: boolean; error?: string }) => { const r = action(); setMessage(r.ok ? "Perubahan tersimpan dan tersedia untuk pesantren ini." : r.error ?? "Perubahan gagal disimpan."); };
  return <section className="flex flex-col gap-5"><header><p className="kicker">Master lokasi</p><h1 className="text-2xl font-extrabold text-heading">Lokasi dan denah</h1><p className="mt-1 text-sm text-secondary-text">Area baru langsung tersedia pada formulir lapor pesantren ini.</p></header>{message && <p role="status" className="rounded border border-line-soft p-3 text-sm">{message}</p>}<div className="grid gap-4 lg:grid-cols-2"><section className="surface p-4"><h2 className="font-bold">Tambah gedung</h2><label className="mt-3 block text-sm font-bold">Kode<input className={field} value={code} onChange={(e) => setCode(e.target.value)} /></label><label className="mt-3 block text-sm font-bold">Nama<input className={field} value={name} onChange={(e) => setName(e.target.value)} /></label><button className="primary-button mt-4" onClick={() => run(() => storeActions.addBuilding(user, { code, name }))}>Tambah gedung</button></section><section className="surface p-4"><h2 className="font-bold">Tambah area</h2><label className="mt-3 block text-sm font-bold">Gedung<select className={field} value={buildingId} onChange={(e) => setBuildingId(e.target.value)}><option value="">Pilih gedung</option>{buildings.map((x) => <option key={x.id} value={x.id}>{x.code} — {x.name}</option>)}</select></label><label className="mt-3 block text-sm font-bold">Lantai<input className={field} value={floor} onChange={(e) => setFloor(e.target.value)} /></label><label className="mt-3 block text-sm font-bold">Zona/blok<input className={field} value={zone} onChange={(e) => setZone(e.target.value)} /></label><label className="mt-3 block text-sm font-bold">Nama area<input className={field} value={area} onChange={(e) => setArea(e.target.value)} /></label><button className="primary-button mt-4" onClick={() => run(() => storeActions.addArea(user, { buildingId, floor, name: area, zone }))}>Tambah area</button></section></div><section className="surface p-4"><h2 className="font-bold">Gedung, lantai, dan versi denah</h2><div className="mt-3 space-y-3">{buildings.map((building) => <article key={building.id} className="rounded border border-line-soft p-3"><h3 className="font-bold">{building.code} · {building.name}</h3><div className="mt-3 grid gap-3 md:grid-cols-2">{building.floors.map((item) => <div key={item.id} className="rounded bg-surface-muted p-3 text-sm"><strong>{item.name}</strong><p className="mt-1 text-secondary-text">{item.planVersion ? `${item.planVersion} · ${item.planFile}` : "Belum ada denah"}</p><input type="file" accept=".jpg,.jpeg,.png,.pdf" className="mt-2 block w-full text-xs" onChange={(e) => { const file = e.currentTarget.files?.[0]; if (file) run(() => storeActions.savePlanVersion(user, building.id, item.id, file.name)); e.currentTarget.value = ""; }} />{item.planHistory?.length ? <p className="mt-2 text-xs text-secondary-text">Riwayat: {item.planHistory.map((x) => x.version).join(", ")}</p> : null}</div>)}</div><div className="mt-3 flex gap-2"><input className="min-h-10 flex-1 rounded border border-line-soft px-3" value={newFloor} onChange={(e) => setNewFloor(e.target.value)} placeholder="Nama lantai baru" /><button className="secondary-button" onClick={() => run(() => storeActions.addFloor(user, building.id, newFloor))}>Tambah lantai</button></div></article>)}</div></section><section className="surface p-4"><h2 className="font-bold">Daftar area</h2><div className="mt-3 grid gap-2 md:grid-cols-2">{areas.map((x) => <div key={x.id} className="rounded border border-line-soft p-3 text-sm"><strong>{x.name}</strong><p className="text-secondary-text">{buildings.find((b) => b.id === x.buildingId)?.name} · {x.floor} · {x.zone}</p></div>)}</div></section></section>;
}
