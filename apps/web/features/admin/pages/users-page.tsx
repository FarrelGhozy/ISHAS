import { useMemo, useState } from "react";
import { Search } from "lucide-react";
import { storeActions, useMockState } from "~/mocks/store/mock-store";
import { EmptyState } from "~/shared/components/empty-state";
import { StatusChip } from "~/shared/components/status-chip";

export function Page() {
  const state = useMockState(); const [name,setName]=useState(""); const [email,setEmail]=useState(""); const [roleId,setRoleId]=useState<"admin"|"peneliti"|"pengelola">("pengelola"); const [institutionCode,setInstitutionCode]=useState(""); const [query,setQuery]=useState(""); const [status,setStatus]=useState("Semua"); const [note,setNote]=useState("");
  const users=useMemo(()=>state.users.filter(x=>(status==="Semua"||x.status===status)&&`${x.name} ${x.email} ${x.role} ${x.institution}`.toLowerCase().includes(query.toLowerCase())),[state.users,status,query]);
  const roleLabel = roleId==="admin"?"Super Admin":roleId==="peneliti"?"Peneliti":"Pengelola Pesantren";
  const add=()=>{const n=Math.max(0,...state.users.map(x=>Number(x.id.replace(/\D/g,""))))+1;const institution=state.institutions.find(x=>x.code===institutionCode);const scopeName=roleId==="pengelola"?(institution?.name??""):roleId==="admin"?"Seluruh sistem":"Seluruh sistem";const r=storeActions.addUser({id:`USR-${String(n).padStart(3,"0")}`,name,email,initials:name.split(" ").filter(Boolean).map(x=>x[0]).join("").slice(0,2).toUpperCase(),role:roleLabel as "Super Admin"|"Peneliti"|"Pengelola Pesantren",roleId,institution:scopeName,institutionCodes:roleId==="pengelola"?(institutionCode?[institutionCode]:[]):[],status:"Aktif",lastActive:new Date().toISOString()});setNote(r.ok?`Akun ${roleLabel} berhasil dibuat.`:r.error);if(r.ok){setName("");setEmail("");setInstitutionCode("");}};
  const changeStatus=(id:string,value:"Aktif"|"Menunggu"|"Nonaktif",label:string)=>{const result=storeActions.setUserStatus(id,value);setNote(result.ok?`Status ${label} diperbarui.`:result.error);};
  return <section className="flex flex-col gap-4"><header><p className="kicker">Administrasi</p><h1 className="text-2xl font-extrabold text-heading">Pengguna</h1><p className="text-sm text-secondary-text">Buat akun Super Admin, Peneliti, dan Pengelola Pesantren (D-09). Pengelola wajib satu pesantren aktif.</p></header><div className="surface p-4"><h2 className="mb-3 font-bold text-heading">Akun baru</h2><div className="grid gap-3 md:grid-cols-5"><label className="text-xs font-bold">Nama<input className="mt-1 min-h-11 w-full rounded border border-line-soft px-3 font-normal" value={name} onChange={e=>setName(e.target.value)} /></label><label className="text-xs font-bold">Email<input type="email" className="mt-1 min-h-11 w-full rounded border border-line-soft px-3 font-normal" value={email} onChange={e=>setEmail(e.target.value)} /></label><label className="text-xs font-bold">Peran<select className="mt-1 min-h-11 w-full rounded border border-line-soft px-3 font-normal" value={roleId} onChange={e=>setRoleId(e.target.value as typeof roleId)}><option value="pengelola">Pengelola Pesantren</option><option value="peneliti">Peneliti</option><option value="admin">Super Admin</option></select></label><label className="text-xs font-bold">Pesantren aktif<select className="mt-1 min-h-11 w-full rounded border border-line-soft px-3 font-normal" value={institutionCode} disabled={roleId!=="pengelola"} onChange={e=>setInstitutionCode(e.target.value)}><option value="">{roleId==="pengelola"?"Pilih pesantren":"Tanpa scope lembaga"}</option>{state.institutions.filter(x=>x.status==="Aktif").map(x=><option key={x.code} value={x.code}>{x.name}</option>)}</select></label><button type="button" className="primary-button self-end" onClick={add}>Buat akun</button></div>{note&&<p role={note.includes("berhasil")||note.includes("diperbarui")?"status":"alert"} className="mt-3 text-sm">{note}</p>}</div><div className="flex flex-wrap gap-2"><label className="relative min-w-60 flex-1"><Search className="absolute left-3 top-3" size={18}/><input aria-label="Cari pengguna" className="min-h-11 w-full rounded border border-line-soft pl-10 pr-3" placeholder="Cari nama, email, peran…" value={query} onChange={e=>setQuery(e.target.value)}/></label><select aria-label="Filter status pengguna" className="min-h-11 rounded border border-line-soft bg-white px-3" value={status} onChange={e=>setStatus(e.target.value)}><option>Semua</option><option>Aktif</option><option>Menunggu</option><option>Nonaktif</option></select></div>{users.length?<div className="surface divide-y divide-line">{users.map(x=><article key={x.id} className="flex flex-wrap items-center gap-3 p-4 text-sm"><div className="mr-auto min-w-48"><strong className="text-heading">{x.name}</strong><p className="text-xs text-secondary-text">{x.email} · {x.role}</p><p className="text-xs text-faint">{x.institution}</p></div><StatusChip value={x.status}/><select aria-label={`Status ${x.name}`} className="min-h-11 rounded border border-line-soft px-2" value={x.status} onChange={e=>changeStatus(x.id,e.target.value as typeof x.status,x.name)}><option>Aktif</option><option>Menunggu</option><option>Nonaktif</option></select></article>)}</div>:<EmptyState title="Pengguna tidak ditemukan" description="Ubah kata pencarian atau filter status."/>}</section>;
}
