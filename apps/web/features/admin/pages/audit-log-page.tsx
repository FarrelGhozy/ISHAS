import { useMemo, useState } from "react";
import { useMockState } from "~/mocks/store/mock-store";
import { EmptyState } from "~/shared/components/empty-state";
export function Page(){const state=useMockState();const[query,setQuery]=useState("");const[type,setType]=useState("Semua");const types=[...new Set(state.auditEvents.map(x=>x.objectType))];const events=useMemo(()=>state.auditEvents.filter(x=>(type==="Semua"||x.objectType===type)&&`${x.action} ${x.actorName} ${x.objectId} ${x.note??""}`.toLowerCase().includes(query.toLowerCase())),[state.auditEvents,query,type]);return <section className="flex flex-col gap-4"><header><p className="kicker">Administrasi</p><h1 className="text-2xl font-extrabold text-heading">Audit log</h1><p className="text-sm text-secondary-text">Jejak perubahan data sistem yang tersimpan secara kronologis.</p></header><div className="flex gap-2"><input aria-label="Cari audit" className="min-h-11 flex-1 rounded border border-line-soft px-3" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Cari aksi, pelaku, objek, atau catatan…"/><select aria-label="Filter objek audit" className="min-h-11 rounded border border-line-soft bg-white px-3" value={type} onChange={e=>setType(e.target.value)}><option>Semua</option>{types.map(x=><option key={x}>{x}</option>)}</select></div>{events.length?<div className="surface divide-y divide-line">{events.map(x=><article key={x.id} className="p-4 text-sm"><div className="flex flex-wrap gap-2"><strong className="mr-auto text-heading">{x.action}</strong><span className="status status-neutral">{x.objectType}</span></div><p className="text-secondary-text">{x.actorName} · {x.objectId} · {new Date(x.at).toLocaleString("id-ID")}</p>{x.note&&<p className="mt-1 text-xs">{x.note}</p>}</article>)}</div>:<EmptyState title="Aktivitas tidak ditemukan" description="Ubah pencarian atau filter objek."/>}</section>}
